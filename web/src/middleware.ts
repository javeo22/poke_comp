// Supabase Auth middleware — disabled until auth is implemented.
// Re-enable when replacing the hardcoded dev user ID with real Supabase Auth.
//
// import { createServerClient } from "@supabase/ssr"
// import { NextResponse, type NextRequest } from "next/server"

export { };
