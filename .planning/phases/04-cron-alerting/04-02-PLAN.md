---
phase: 04-cron-alerting
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [api/app/routers/admin_cron.py, LEGAL_AND_DEV_GUIDELINES.md, web/src/app/privacy/page.tsx]
autonomous: false
requirements: [NFR-meta-freshness-pipeline]

must_haves:
  truths:
    - "A cron failure triggers an out-of-band notification"
    - "Legal ledger reflects Slack/Discord as a third-party recipient"
    - "Privacy policy discloses the new notification service"
  artifacts:
    - path: "api/app/routers/admin_cron.py"
      provides: "Alerting integration points"
    - path: "web/src/app/privacy/page.tsx"
      provides: "Public privacy disclosure"
  key_links:
    - from: "api/app/routers/admin_cron.py"
      to: "api/app/services/alerting.py"
      via: "invoking send_alert on failure"
---

<objective>
Integrate the alerting service into the cron execution flow, update legal documentation, and perform end-to-end verification.

Purpose: To close the loop on automated monitoring and maintain legal compliance.
Output: Integrated alerting, updated legal docs, and a successful failure-injection test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cron-alerting/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cron Integration & Test Endpoint</name>
  <files>api/app/routers/admin_cron.py</files>
  <action>
    - Import `send_alert` from `app.services.alerting`.
    - Update `_record_cron_run` to call `send_alert` when `status == "fail"`.
    - Format the alert message to include source name and the error summary.
    - Add a GET `/test-alert` endpoint to the `admin-cron` router (requires `require_cron_secret`).
    - The `/test-alert` endpoint should explicitly raise an `Exception("Deliberate failure for alerting test")`.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $CRON_SECRET" http://localhost:8000/api/admin/cron/test-alert | grep "500"</automated>
  </verify>
  <done>Cron execution flow now includes automated alerting triggers.</done>
</task>

<task type="auto">
  <name>Task 2: Compliance & Privacy Updates</name>
  <files>LEGAL_AND_DEV_GUIDELINES.md, web/src/app/privacy/page.tsx</files>
  <action>
    - Update `LEGAL_AND_DEV_GUIDELINES.md`: Add "Slack/Discord (Incoming Webhooks)" to the "Third-Party Data Recipients" table. Purpose: "Internal operational alerting". Data Sent: "Cron failure logs (no user PII)".
    - Update `web/src/app/privacy/page.tsx`: Add a note about Slack/Discord in Section 3 (Third-Party Services) for operational monitoring.
  </action>
  <verify>
    <automated>grep "Slack/Discord" LEGAL_AND_DEV_GUIDELINES.md && grep "Slack/Discord" web/src/app/privacy/page.tsx</automated>
  </verify>
  <done>Legal and privacy documentation is current with the new system architecture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end alerting integration.</what-built>
  <how-to-verify>
    1. Set a valid `ALERT_WEBHOOK_URL` (using a test Slack/Discord channel) in your `.env`.
    2. Start the API.
    3. Trigger the test alert: `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:8000/api/admin/cron/test-alert`.
    4. Confirm that a notification arrives in the Slack/Discord channel.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal → Slack/Discord | Data transfer to third-party. Restricted to non-PII operational logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Privacy Violation | `api/app/routers/admin_cron.py` | mitigate | Ensure alerts only contain source names and exception types; no user PII flows into Slack/Discord. |
</threat_model>

<verification>
- Successful receipt of a test alert in a real webhook target.
</verification>

<success_criteria>
- Cron failures trigger Slack/Discord alerts.
- Privacy policy and legal guidelines are updated.
- `/admin/cron/test-alert` works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cron-alerting/04-02-SUMMARY.md`
</output>
