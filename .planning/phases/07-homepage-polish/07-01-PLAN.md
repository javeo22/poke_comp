---
phase: 07-homepage-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "web/src/features/meta/baseline-trends.ts",
  "web/src/components/ui/lab-dashboard.tsx",
  "web/src/app/page.tsx"
]
autonomous: true
requirements: [REQ-ui-polish, REQ-data-resiliency]
must_haves:
  truths:
    - "Homepage never looks 'empty' even without API data"
    - "Lab Dashboard aesthetic (grid/scan-lines) is applied to the homepage"
    - "Background mascots are present but non-intrusive"
  artifacts:
    - path: "web/src/features/meta/baseline-trends.ts"
      provides: "Fallback meta data"
    - path: "web/src/components/ui/lab-dashboard.tsx"
      provides: "Dashboard layout container"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "web/src/components/ui/lab-dashboard.tsx"
      via: "import and wrap"
---

<objective>
Establish the foundation for the "Lab Dashboard" aesthetic by creating the core background components and defining a resilient set of baseline trends for the homepage.

Purpose: Fix the "empty" look of the homepage and professionalize the UX.
Output: Baseline data constant and the `LabDashboard` visual system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-homepage-polish/07-RESEARCH.md
@api/scripts/seed_champions.py
</context>

<tasks>

<task type="auto" status="completed">
  <name>Define Baseline Trends</name>
  <files>web/src/features/meta/baseline-trends.ts</files>
  <action>
    Create `web/src/features/meta/baseline-trends.ts`.
    Define and export a constant `BASELINE_TRENDS: MetaTrend[]`.
    Use the following data (derived from Champions Season M-1 / INITIAL_TIER_DATA):
    - Incineroar (727): 78.4% USG, 54.2% WR, Role: "Intimidate Pivot"
    - Kingambit (983): 42.1% USG, 51.8% WR, Role: "Late-game Sweeper"
    - Garchomp (445): 31.5% USG, 49.2% WR, Role: "Physical Pressure"
    - Dragonite (149): 28.2% USG, 52.1% WR, Role: "Multiscale Sweeper"
    - Glimmora (970): 24.5% USG, 48.7% WR, Role: "Hazard Setter"
    - Sinistcha (1013): 22.8% USG, 53.5% WR, Role: "Support / Redirect"

    Ensure the structure matches the `MetaTrend` interface in `web/src/types/meta.ts`.
  </action>
  <verify>
    <automated>test -f web/src/features/meta/baseline-trends.ts && grep -q "BASELINE_TRENDS" web/src/features/meta/baseline-trends.ts</automated>
  </verify>
  <done>BASELINE_TRENDS constant is defined with accurate Champions-eligible IDs.</done>
</task>

<task type="auto" status="completed">
  <name>Create LabDashboard Component</name>
  <files>web/src/components/ui/lab-dashboard.tsx</files>
  <action>
    Implement `LabDashboard` in `web/src/components/ui/lab-dashboard.tsx`.
    
    Requirements:
    - A `lab-dashboard-bg` class (defined in globals.css or using Tailwind inline) that implements a magenta-tinted dot-grid (20px).
    - A `scan-lines` overlay (repeating-linear-gradient).
    - Two background mascots (Incineroar 727 and Kingambit 983) using `next/image`.
    - Mascot constraints: `opacity-10`, `grayscale`, `blur-sm`, `pointer-events-none`.
    - Layout: Position Incineroar bottom-left, Kingambit top-right (partially out of frame).
    - Props: `children: React.ReactNode`.
    
    Refer to `07-RESEARCH.md` for the CSS patterns.
  </action>
  <verify>
    <automated>test -f web/src/components/ui/lab-dashboard.tsx && grep -q "LabDashboard" web/src/components/ui/lab-dashboard.tsx</automated>
  </verify>
  <done>LabDashboard component exists with grid, scan-lines, and background mascots.</done>
</task>

<task type="auto" status="completed">
  <name>Integrate Dashboard into Homepage</name>
  <files>web/src/app/page.tsx</files>
  <action>
    Modify `web/src/app/page.tsx`:
    1. Import `LabDashboard` and wrap the entire page content.
    2. Import `BASELINE_TRENDS` and use it as the fallback/initial state for `trends` if `fetchMetaTrends` returns empty or fails.
    3. Update the Hero section "Active" banner:
       - Text: "REGULATION M-A DASHBOARD"
       - Subtext: "LIVE TELEMETRY FROM SEASON M-1 CHAMPIONS"
    4. Ensure no visual regressions and that the background mascots are properly dimmed behind the content.
  </action>
  <verify>
    <automated>grep -q "LabDashboard" web/src/app/page.tsx && grep -q "REGULATION M-A DASHBOARD" web/src/app/page.tsx</automated>
  </verify>
  <done>Homepage is wrapped in LabDashboard and displays the new telemetry header.</done>
</task>


</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | External trends data is untrusted; fallback data is hardcoded (trusted). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | baseline-trends.ts | mitigate | Data is hardcoded in source; protected by git/code-review. |
</threat_model>

<verification>
Manual verification required:
1. Run `pnpm dev` in `web/` directory.
2. Visit `http://localhost:3000`.
3. Confirm "Lab Dashboard" aesthetic (grid, scan-lines) is visible.
4. Confirm background mascots are present but subtle.
5. Confirm "REGULATION M-A DASHBOARD" header is visible.
6. Verify that baseline trends are used if API is offline.
</verification>

<success_criteria>
- [ ] Lab Dashboard visual system implemented.
- [ ] Baseline trends available and used as fallback.
- [ ] Homepage aesthetic is professional and high-density.
- [ ] No layout breakage from background effects.
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-polish/07-01-SUMMARY.md`
</output>
