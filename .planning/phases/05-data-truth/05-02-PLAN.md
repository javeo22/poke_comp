---
phase: 05-data-truth
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [api/scripts/ingest/limitless_teams.py, api/scripts/ingest/pikalytics_usage.py]
autonomous: true
requirements: [F11, F4]
must_haves:
  truths:
    - "Limitless scraper stages tournament data to the review queue instead of inserting to tournament_teams."
    - "Pikalytics scraper stages usage data to the review queue instead of updating pokemon_usage."
  artifacts:
    - path: "api/scripts/ingest/limitless_teams.py"
      provides: "Refactored staging-first logic"
    - path: "api/scripts/ingest/pikalytics_usage.py"
      provides: "Refactored staging-first logic"
  key_links:
    - from: "api/scripts/ingest/limitless_teams.py"
      to: "api/app/services/classifier.py"
      via: "import and call"
---

<objective>
Refactor existing scrapers to adopt the HITL staging pattern.
Instead of direct insertion into production tables, they will now classify data and stage it in the review queue.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-truth/05-01-SUMMARY.md
@api/scripts/ingest/limitless_teams.py
@api/scripts/ingest/pikalytics_usage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Limitless Scraper for Staging</name>
  <files>api/scripts/ingest/limitless_teams.py</files>
  <action>
    - Import `TournamentClassifier` and `ReviewService`.
    - In `ingest_limitless_tournaments`:
        - Fetch tournament metadata from Limitless (if available).
        - Call `TournamentClassifier.classify()`.
        - For each team record, call `ReviewService.stage_item()` with the team payload and the AI metadata.
        - Remove the direct `sb.table("tournament_teams").insert()` logic.
    - Update `dry_run` to show staging intent.
  </action>
  <verify>
    <automated>uv run python -m scripts.ingest.limitless_teams --dry-run</automated>
  </verify>
  <done>Limitless teams are staged for review.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Pikalytics Scraper for Staging</name>
  <files>api/scripts/ingest/pikalytics_usage.py</files>
  <action>
    - Import `ReviewService`.
    - In `ingest_pikalytics`:
        - For each Pokemon usage record, call `ReviewService.stage_item()` with the usage payload.
        - Remove the direct `sb.table("pokemon_usage").upsert()` and `delete()` logic.
    - Update `dry_run` to show staging intent.
  </action>
  <verify>
    <automated>uv run python -m scripts.ingest.pikalytics_usage --dry-run</automated>
  </verify>
  <done>Pikalytics usage stats are staged for review.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Scraper payload | mitigate | Scrapers use shared `ReviewService` which enforces payload validation. |
</threat_model>

<verification>
Run scrapers in `--dry-run` and verify logs show staging calls instead of DB insertions.
Check `scraper_review_queue` table for records after a non-dry run.
</verification>

<success_criteria>
- [ ] Limitless scraper calls the AI classifier.
- [ ] No direct writes to `tournament_teams` or `pokemon_usage` from scrapers.
- [ ] All scraper output lands in `scraper_review_queue` with status 'PENDING'.
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-truth/05-02-SUMMARY.md`
</output>
