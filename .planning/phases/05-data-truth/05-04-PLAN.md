---
phase: 05-data-truth
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified: [api/app/routers/meta.py, web/src/app/page.tsx, web/src/components/cheatsheet/cheatsheet-content.tsx]
autonomous: true
requirements: [F4]
must_haves:
  truths:
    - "Homepage meta preview shows real data from the database."
    - "Meta trends include up/down 'swing' calculated via SQL window functions."
    - "Cheatsheet design is consistent with the homepage esports aesthetic."
  artifacts:
    - path: "api/app/routers/meta.py"
      provides: "/api/meta/trends endpoint"
    - path: "web/src/components/cheatsheet/cheatsheet-content.tsx"
      provides: "Unified esports theme"
  key_links:
    - from: "web/src/app/page.tsx"
      to: "/api/meta/trends"
      via: "fetch"
---

<objective>
Finalize the Phase 5 goal by making the homepage dynamic and unifying the UI design language.
The meta trends section will now reflect real database state with calculated weekly swings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/app/routers/meta.py
@web/src/app/page.tsx
@web/src/components/cheatsheet/cheatsheet-content.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Dynamic Meta Trends Endpoint</name>
  <files>api/app/routers/meta.py</files>
  <behavior>
    - `GET /api/meta/trends` returns top 6 Pokemon by usage.
    - Each record includes current usage, win rate, and `swing` (usage delta vs previous snapshot).
    - Swing calculated via SQL window function `LAG() OVER`.
  </behavior>
  <action>
    Update `api/app/routers/meta.py` to add the `/trends` endpoint.
    Use an RPC or complex raw SQL query to fetch the current vs previous usage snapshots for the top 6 Pokemon.
    Calculate `swing = current_usage - previous_usage`.
    Set `up: boolean` based on swing direction.
  </action>
  <verify>
    <automated>curl -f http://localhost:8000/api/meta/trends</automated>
  </verify>
  <done>Dynamic meta data available via API.</done>
</task>

<task type="auto">
  <name>Task 2: Connect Homepage to Meta Trends API</name>
  <files>web/src/app/page.tsx</files>
  <action>
    - Replace the hardcoded `META_PREVIEW` constant with a `useState` and `useEffect` fetch to `/api/meta/trends`.
    - Ensure loading states are handled (render skeletons or empty state if no data).
    - Map the API response to the existing card layout.
  </action>
  <verify>
    <automated>ls web/src/app/page.tsx</automated>
  </verify>
  <done>Homepage shows live meta trends.</done>
</task>

<task type="auto">
  <name>Task 3: Unify Cheatsheet Design with Esports Theme</name>
  <files>web/src/components/cheatsheet/cheatsheet-content.tsx</files>
  <action>
    - Apply the "esports broadcast" aesthetic from the homepage to the cheatsheet.
    - Update colors to use higher contrast/glow (e.g., `rgba(255,45,122,0.4)` for accents).
    - Adopt the mono-font headers and pulse/glow elements where appropriate.
    - Ensure readability for the "A4 Print" use case is preserved or enhanced.
  </action>
  <verify>
    <automated>ls web/src/components/cheatsheet/cheatsheet-content.tsx</automated>
  </verify>
  <done>Cheatsheet UI is unified with the rest of the app's premium design.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Denial of Service | /api/meta/trends | mitigate | Add rate limiting to the trends endpoint via SlowAPI. |
</threat_model>

<verification>
Check Homepage for dynamic data. Verify Cheatsheet UI looks consistent with Homepage.
</verification>

<success_criteria>
- [ ] Homepage `META MOVERS` section fetches data from `/api/meta/trends`.
- [ ] Usage swings are accurately calculated from DB history.
- [ ] Cheatsheet UI uses the new design language.
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-truth/05-04-SUMMARY.md`
</output>
