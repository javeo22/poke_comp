---
phase: 05-data-truth
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: [api/app/routers/admin_review.py, api/app/main.py, web/src/app/admin/review/page.tsx, web/src/lib/api.ts]
autonomous: false
requirements: [F11, F4]
must_haves:
  truths:
    - "Admin can view PENDING scraper records in a web interface."
    - "Admin can approve or reject records, triggering the review service."
    - "Admin UI shows AI classification results (metadata) to assist review."
  artifacts:
    - path: "api/app/routers/admin_review.py"
      provides: "REST endpoints for HITL review"
    - path: "web/src/app/admin/review/page.tsx"
      provides: "HITL Dashboard UI"
  key_links:
    - from: "web/src/app/admin/review/page.tsx"
      to: "/api/admin/review"
      via: "fetch"
---

<objective>
Build the Human-in-the-Loop (HITL) review dashboard.
This allows administrators to manually verify scraped data before it moves to production tables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-truth/05-01-SUMMARY.md
@.planning/phases/05-data-truth/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Admin Review API Router</name>
  <files>api/app/routers/admin_review.py, api/app/main.py</files>
  <action>
    - Create `api/app/routers/admin_review.py` with endpoints:
        - `GET /api/admin/review/pending`: List PENDING records.
        - `POST /api/admin/review/{id}/approve`: Call `ReviewService.approve_item()`.
        - `POST /api/admin/review/{id}/reject`: Call `ReviewService.reject_item()`.
    - Apply `is_admin` middleware or dependency check.
    - Register router in `api/app/main.py`.
  </action>
  <verify>
    <automated>curl -f http://localhost:8000/api/admin/review/pending -H "Authorization: Bearer ADMIN_TOKEN"</automated>
  </verify>
  <done>Admin review API is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Build Admin Review Dashboard UI</name>
  <files>web/src/app/admin/review/page.tsx, web/src/lib/api.ts</files>
  <action>
    - Add review endpoints to `web/src/lib/api.ts`.
    - Create `web/src/app/admin/review/page.tsx`:
        - Fetch pending records on load.
        - Render a table/list showing: source, external_id, AI classification (category/reason), payload preview.
        - Buttons for "Approve" and "Reject".
        - "Select All" + "Bulk Approve" (high confidence > 0.9) per research recommendation.
  </action>
  <verify>
    <automated>ls web/src/app/admin/review/page.tsx</automated>
  </verify>
  <done>Admin review UI is ready for human testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Admin Review Dashboard</what-built>
  <how-to-verify>
    1. Run a scraper (e.g., `uv run python -m scripts.ingest.limitless_teams`) to populate the queue.
    2. Visit http://localhost:3000/admin/review.
    3. Verify pending records appear with AI classification metadata.
    4. Click "Approve" on a record and verify it disappears from the queue and appears in the Meta tab.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | /admin/review/approve | mitigate | Backend checks `is_admin` flag in JWT/DB before processing approval. |
</threat_model>

<verification>
Manual verification of the review flow.
</verification>

<success_criteria>
- [ ] Admin can see pending scraper data in the UI.
- [ ] Approving a record moves it to the production database.
- [ ] Rejecting a record removes it from the queue.
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-truth/05-03-SUMMARY.md`
</output>
