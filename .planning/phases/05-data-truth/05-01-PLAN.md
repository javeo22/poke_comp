---
phase: 05-data-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260603000000_scraper_review_queue.sql, api/app/services/classifier.py, api/app/services/review_service.py, api/tests/test_classifier.py, api/tests/test_review_logic.py]
autonomous: true
requirements: [F11, F4]
must_haves:
  truths:
    - "A reviewer can query pending scraper records in the database."
    - "The AI classifier can distinguish between Champions and Standard VGC formats."
    - "The review service can move records from the queue to production tables upon approval."
  artifacts:
    - path: "supabase/migrations/20260603000000_scraper_review_queue.sql"
      provides: "Staging table and RLS"
    - path: "api/app/services/classifier.py"
      provides: "Claude-powered tournament classification"
    - path: "api/app/services/review_service.py"
      provides: "Logic for queue-to-live data movement"
  key_links:
    - from: "api/app/services/review_service.py"
      to: "scraper_review_queue"
      via: "supabase.table"
---

<objective>
Establish the technical foundation for the Data Truth & HITL (Human-in-the-Loop) system.
This includes the database schema for staging, the AI classifier for tournament filtering, and the core review logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-truth/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQL Migration for Scraper Review Queue</name>
  <files>supabase/migrations/20260603000000_scraper_review_queue.sql</files>
  <behavior>
    - Table `scraper_review_queue` exists with columns: id, source, payload (jsonb), status (PENDING/APPROVED/REJECTED), external_id, metadata (jsonb), reviewed_at, reviewed_by, created_at.
    - RLS enabled: admin-only access for SELECT/UPDATE/DELETE.
  </behavior>
  <action>
    Create the migration file per the research schema. Ensure `status` has a check constraint for ['PENDING', 'APPROVED', 'REJECTED'].
    Set up RLS policies so that only users with `is_admin = true` in `user_profiles` can view or modify the queue.
  </action>
  <verify>
    <automated>ls supabase/migrations/20260603000000_scraper_review_queue.sql</automated>
  </verify>
  <done>Staging table created with proper RLS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Tournament Classifier Service</name>
  <files>api/app/services/classifier.py, api/tests/test_classifier.py</files>
  <behavior>
    - `TournamentClassifier.classify(name, description)` returns a category (CHAMPIONS, VGC_STANDARD, OTHER) and confidence.
    - Uses Claude 3.5 Sonnet via the Anthropic SDK.
    - Handles missing descriptions gracefully.
  </behavior>
  <action>
    Implement `TournamentClassifier` using the prompt logic from 05-RESEARCH.md.
    Ensure it uses `anthropic.AsyncAnthropic` and handles JSON parsing of the AI response.
    Create a test fixture with 3-5 sample tournament names/descriptions (including "Champions" format variants).
  </action>
  <verify>
    <automated>pytest api/tests/test_classifier.py</automated>
  </verify>
  <done>AI classifier accurately categorizes tournament formats.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Review Service Logic</name>
  <files>api/app/services/review_service.py, api/tests/test_review_logic.py</files>
  <action>
    Create a `ReviewService` with methods:
    - `stage_item(source, payload, external_id, metadata)`: Inserts a PENDING record.
    - `approve_item(review_id, admin_user_id)`: Moves data from payload into `tournament_teams` (for Limitless) or `pokemon_usage` (for Pikalytics) and marks as APPROVED.
    - `reject_item(review_id, admin_user_id)`: Marks as REJECTED.
    Ensure it handles the different payload structures for teams vs usage stats.
  </action>
  <verify>
    <automated>pytest api/tests/test_review_logic.py</automated>
  </verify>
  <done>Review service handles the full staging-to-live lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scraper → Staging | External data enters the staging area. |
| Staging → Live | Admin-approved data moves to production tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | scraper_review_queue.payload | mitigate | Validate JSON schema via Pydantic before staging and during approval. |
| T-05-01-02 | Elevation of Privilege | /admin/review endpoints | mitigate | Enforce is_admin check in review service and RLS. |
</threat_model>

<verification>
Run `pytest api/tests/test_classifier.py` and `pytest api/tests/test_review_logic.py` to ensure core logic is sound.
</verification>

<success_criteria>
- [ ] `scraper_review_queue` table exists in Supabase.
- [ ] `TournamentClassifier` returns correct categories for test cases.
- [ ] `ReviewService` successfully moves a staged "team" payload into `tournament_teams`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-truth/05-01-SUMMARY.md`
</output>
