# Plan 08-01: Database Fix & Format Consolidation

## Goal
Resolve the 'missing table' error in the review queue and streamline the application by removing the redundant 'Megas' format, focusing exclusively on Singles and Doubles.

## Proposed Changes

### 1. Database & Admin Fix
- **Action:** Set `is_admin = true` for the developer user in `user_profiles` to resolve the RLS policy block on `scraper_review_queue`.
- **File:** `api/scripts/seed_auth_user.py` (ensure it sets admin flag) or manual SQL run via Supabase.

### 2. Format Pruning (Backend)
- **Action:** Remove `'megas'` from the `format` Literal and regex patterns.
- **Files:**
    - `api/app/models/meta.py`
    - `api/app/models/team.py`
    - `api/app/routers/meta.py`
    - `api/app/routers/teams.py`
    - `api/app/routers/cheatsheet.py`

### 3. Format Pruning (Frontend)
- **Action:** Remove `'megas'` from meta and team constants.
- **Files:**
    - `web/src/types/meta.ts`
    - `web/src/types/team.ts`
    - `web/src/components/teams/team-card.tsx`

## Verification Plan
- [ ] **Admin Check:** Confirm the Review page (`/admin/review`) loads without the 'public.scraper_review_queue' error.
- [ ] **Type Check:** Run `tsc` and `pyright` to ensure no stale 'megas' references remain.
- [ ] **UI Check:** Verify the "Format" dropdowns no longer show "Megas".
