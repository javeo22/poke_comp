---
phase: 03-rag-augmentation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [api/tests/test_rag.py, api/scripts/benchmark_rag.py]
autonomous: false
requirements: [REQ-rag-augmentation, REQ-rag-implementation-tasks]

must_haves:
  truths:
    - "Dual RAG integration tests pass with mock fixtures"
    - "Retrieval latency is under 50ms for parallel context fetching"
    - "Claude correctly acknowledges injected history/tournament context in UAT"
  artifacts:
    - path: "api/tests/test_rag.py"
      provides: "Integration tests for RAG pipeline"
    - path: "api/scripts/benchmark_rag.py"
      provides: "Latency benchmarking script"
---

<objective>
Verify the Dual RAG implementation through integration tests, latency benchmarking, and quality checks.

Purpose: Ensure the new system meets the performance budget (< 50ms) and provides the intended strategic value without regression.
Output: Test suite and benchmark results.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/app/routers/draft.py
@api/app/services/retrieval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RAG Integration Tests</name>
  <files>api/tests/test_rag.py</files>
  <action>
    Create integration tests that:
    1. Seed mock `matchup_log` and `tournament_teams` data.
    2. Mock the Anthropic API call.
    3. Verify that `analyze_draft` correctly triggers context retrieval.
    4. Assert that the generated prompt (captured via mock) contains the expected XML blocks.
  </action>
  <verify>
    <automated>pytest api/tests/test_rag.py</automated>
  </verify>
  <done>Integration tests prove context injection logic is working.</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark Retrieval Latency</name>
  <files>api/scripts/benchmark_rag.py</files>
  <action>
    Create a script to measure:
    1. Time taken for `fetch_tournament_context`.
    2. Time taken for `fetch_personal_context`.
    3. Total overhead added to the `analyze_draft` flow.
    
    The script should run 10 iterations and report p50/p95 latency.
  </action>
  <verify>
    <automated>uv run python api/scripts/benchmark_rag.py</automated>
  </verify>
  <done>Latency benchmarking confirms compliance with < 50ms budget.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT of Claude Draft Strategy</name>
  <files>N/A</files>
  <action>Human verification of AI output quality with retrieved context.</action>
  <what-built>Refactored AI Draft Helper with Dual RAG context injection.</what-built>
  <how-to-verify>
    1. Log in to the application.
    2. Ensure you have at least 5 matches recorded in Matchup Log against a specific archetype (e.g., "Rain").
    3. Use the Draft Helper against a team with that same archetype.
    4. Verify that Claude's summary or strategy notes reference your "past performance" or "historical matches."
    5. Verify that Claude mentions "Tournament Archetype" context if available.
  </how-to-verify>
  <verify>Manual verification by user.</verify>
  <done>Claude acknowledges history/tournament context.</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing Environment | Tests use mock data but interact with DB service layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Test Fixtures | mitigate | Ensure test data is cleaned up or uses a separate schema/prefixed IDs to avoid polluting production logs. |
| T-03-06 | Information Disclosure | Retrieval Logs | mitigate | Ensure retrieval service does not log sensitive personal match notes in plain text at high log levels. |
</threat_model>

<verification>
Run all tests and benchmark script. Verify UAT results with a real Claude session if possible.
</verification>

<success_criteria>
1. All RAG integration tests pass.
2. Latency p95 < 50ms.
3. Human-verified quality improvement in Claude's strategy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag-augmentation/03-03-SUMMARY.md`
</output>
