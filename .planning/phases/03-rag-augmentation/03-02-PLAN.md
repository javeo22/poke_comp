---
phase: 03-rag-augmentation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [api/app/routers/draft.py]
autonomous: true
requirements: [REQ-rag-augmentation, REQ-rag-implementation-tasks]

must_haves:
  truths:
    - "AI Draft Helper prompt includes <limitless_pro_context> when data exists"
    - "AI Draft Helper prompt includes <user_personal_context> when data exists"
    - "Personal history notes are sanitized via prompt_guard"
    - "Empty context blocks are never emitted to Claude"
  artifacts:
    - path: "api/app/routers/draft.py"
      provides: "Augmented prompt logic"
  key_links:
    - from: "api/app/routers/draft.py"
      to: "api/app/services/retrieval.py"
      via: "function call"
---

<objective>
Refactor the AI Draft Helper to support Dual RAG context injection using XML blocks.

Purpose: Providing personalized and tournament-specific context allows Claude to give much more relevant and strategic advice.
Output: Refactored `draft.py` with augmented prompt engineering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rag-augmentation/03-RESEARCH.md
@rag-architecture.md
@api/app/services/retrieval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor draft.py Context Fetching</name>
  <files>api/app/routers/draft.py</files>
  <action>
    1. Update imports to include `from app.services.retrieval import fetch_tournament_context, fetch_personal_context`.
    2. Replace the old suboptimal `_fetch_personal_context` and `_fetch_tournament_context` with new versions that:
       - Call the retrieval service.
       - Format the results into structured strings (returning "" if no results).
       - Ensure `fetch_personal_context` uses `sanitize_user_text` on historical match notes.
    3. Ensure the XML block formation happens in these functions or a dedicated formatter.
  </action>
  <verify>
    <automated>grep -q "from app.services.retrieval import" api/app/routers/draft.py</automated>
  </verify>
  <done>Draft router updated to use the high-performance retrieval service.</done>
</task>

<task type="auto">
  <name>Task 2: Update _build_prompt for XML Injection</name>
  <files>api/app/routers/draft.py</files>
  <action>
    Update `_build_prompt` to:
    1. Accept the new context strings.
    2. Inject them into the prompt using Anthropic-standard XML tags:
       - `<limitless_pro_context>` for tournament data.
       - `<user_personal_context>` for history data.
    3. Ensure no empty blocks are emitted (C-SPEC-RAG-EMPTY-STATE).
    4. Update the prompt header to instruct Claude on how to use these context blocks.
  </action>
  <verify>
    <automated>grep -q "limitless_pro_context" api/app/routers/draft.py && grep -q "user_personal_context" api/app/routers/draft.py</automated>
  </verify>
  <done>Prompt engineering updated with XML context blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → LLM | Personal match notes are retrieved from DB and sent to Anthropic; must be sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `_build_prompt` | mitigate | Apply `sanitize_user_text` to all user-sourced notes from `matchup_log`. |
| T-03-04 | Tampering | Prompt Injection | mitigate | Use XML blocks as recommended by Anthropic to maintain clear boundaries between system instructions and data context. |
</threat_model>

<verification>
Verify that context blocks are only added when data is present. Verify that notes are sanitized.
</verification>

<success_criteria>
1. Draft helper successfully retrieves and injects tournament/personal context.
2. Prompt follows XML structure.
3. No empty context blocks sent to LLM.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag-augmentation/03-02-SUMMARY.md`
</output>
