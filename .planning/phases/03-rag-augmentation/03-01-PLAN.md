---
phase: 03-rag-augmentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260602000000_rag_retrieval_foundation.sql, api/app/services/retrieval.py]
autonomous: true
requirements: [REQ-rag-augmentation, REQ-rag-implementation-tasks]

must_haves:
  truths:
    - "GIN indexes exist on tournament_teams.pokemon_ids and matchup_log.opponent_team_data"
    - "Supabase RPCs for similarity retrieval are callable via service key"
    - "Retrieval service can fetch tournament context based on team overlap"
    - "Retrieval service can fetch personal context based on user history overlap"
  artifacts:
    - path: "supabase/migrations/20260602000000_rag_retrieval_foundation.sql"
      provides: "GIN indexes and Supabase RPCs"
    - path: "api/app/services/retrieval.py"
      provides: "Python interface for DB similarity queries"
  key_links:
    - from: "api/app/services/retrieval.py"
      to: "supabase.rpc"
      via: "PostgREST call"
---

<objective>
Implement the technical foundation for Dual RAG: database-level similarity searches and a FastAPI service layer.

Purpose: High-performance retrieval is the core of RAG; moving similarity logic to SQL ensures scalability (< 50ms latency) and precision.
Output: SQL migration (indexes + RPCs) and a new retrieval service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rag-augmentation/03-RESEARCH.md
@rag-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Database Migration for RAG Indexes and RPCs</name>
  <files>supabase/migrations/20260602000000_rag_retrieval_foundation.sql</files>
  <action>
    Create a new migration that:
    1. Adds a GIN index to `tournament_teams` on `pokemon_ids` (INT[]).
    2. Adds a GIN index to `matchup_log` on `opponent_team_data` (JSONB).
    3. Implements Supabase RPC `get_similar_tournament_teams(p_pokemon_ids INT[])` that returns the top 5 matches with highest overlap, sorted by placement (asc) and created_at (desc).
    4. Implements Supabase RPC `get_similar_matchups(p_user_id UUID, p_opponent_names TEXT[])` that returns the top 5 matches with highest overlap in `opponent_team_data`, sorted by overlap count (desc) and played_at (desc).
    
    Use `jsonb_array_elements` for the matchup similarity count as researched in RESEARCH.md.
  </action>
  <verify>
    <automated>grep -q "CREATE INDEX.*idx_tournament_teams_pokemon_ids_gin" supabase/migrations/20260602000000_rag_retrieval_foundation.sql && grep -q "CREATE OR REPLACE FUNCTION get_similar_matchups" supabase/migrations/20260602000000_rag_retrieval_foundation.sql</automated>
  </verify>
  <done>Migration file created with required indexes and RPCs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Retrieval Service Layer</name>
  <files>api/app/services/retrieval.py</files>
  <action>
    Create `api/app/services/retrieval.py` containing:
    1. `fetch_tournament_context(pokemon_ids: list[int], limit: int = 5) -> list[dict]`: Calls `get_similar_tournament_teams` RPC.
    2. `fetch_personal_context(user_id: str, opponent_names: list[str], limit: int = 5) -> list[dict]`: Calls `get_similar_matchups` RPC.
    
    The service should handle Supabase exceptions and return empty lists on failure to satisfy C-SPEC-RAG-EMPTY-STATE.
  </action>
  <verify>
    <automated>uv run python -c "from app.services.retrieval import fetch_tournament_context, fetch_personal_context; print('Imports pass')"</automated>
  </verify>
  <done>FastAPI service layer implemented for context retrieval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Supabase | Service-role access to RPCs; p_user_id must be validated against auth.uid() in RPC or passed explicitly from auth middleware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `get_similar_matchups` | mitigate | Ensure RPC uses `p_user_id` parameter to filter results, complementing RLS. |
| T-03-02 | Denial of Service | Similarity RPCs | mitigate | Use GIN indexes to prevent sequential scans on large datasets. |
</threat_model>

<verification>
Check for existence of migration file and service file. Verify that the RPC signatures match the Python calls.
</verification>

<success_criteria>
1. GIN indexes and RPCs are defined in a new migration.
2. `retrieval.py` provides clean async-compatible wrappers for context fetching.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rag-augmentation/03-01-SUMMARY.md`
</output>
